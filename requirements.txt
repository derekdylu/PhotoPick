# Runtime deps for the Mac app.
# The CLI alone (raw_organizer.py without --gui) needs only the standard library.
PySide6>=6.6
rawpy>=0.19
Pillow>=10.0
send2trash>=1.8

# Dev / packaging
# briefcase>=0.3.17
